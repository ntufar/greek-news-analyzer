version: '3.8'

services:
  epap:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
    depends_on:
      - epap
    restart: unless-stopped
